bitmex:
  pair_url: "https://www.bitmex.com/api/v1/instrument/activeIntervals"
  socket_url: "wss://www.bitmex.com/realtime"

  # 订阅 Depth 数据
  depth_info: [{'depth': '{"op": "subscribe", "args": ["orderBookL2:{symbol}"]}'}]
  utype: "string"
  proxy: "true"
